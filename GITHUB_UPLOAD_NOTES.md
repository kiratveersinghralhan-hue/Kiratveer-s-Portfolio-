# GitHub upload notes

Upload or commit the contents of this folder, not a zip file.

The videos in this folder were compressed for repository upload while keeping the same filenames used by the website:

- `main-demo.mp4`
- `gb1.mp4`
- `manali.mp4`

If GitHub still reports a file-size issue, remove the PNG source/fallback images and keep the `.webp` files, because the live site uses WebP first.
